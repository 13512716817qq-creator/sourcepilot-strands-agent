[project]
name = "sourcepilot-strands-agent"
version = "0.1.0"
description = "Procurement decision engine built with Strands Agents"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
